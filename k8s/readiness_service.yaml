apiVersion: apps/v1
kind: Deployment
metadata:
  name: readiness-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: readiness-service
  template:
    metadata:
      labels:
        app: readiness-service
    spec:
      containers:
        - name: readiness-service
          image: readiness-service:latest
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              value: postgres://testuser:testpassword@postgres/testdb
---
apiVersion: v1
kind: Service
metadata:
  name: readiness-service
  namespace: default
spec:
  selector:
    app: readiness-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
