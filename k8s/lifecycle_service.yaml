apiVersion: apps/v1
kind: Deployment
metadata:
  name: lifecycle-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lifecycle-service
  template:
    metadata:
      labels:
        app: lifecycle-service
    spec:
      containers:
        - name: lifecycle-service
          image: lifecycle-service:latest
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              value: postgres://testuser:testpassword@postgres/testdb
---
apiVersion: v1
kind: Service
metadata:
  name: lifecycle-service
  namespace: default
spec:
  selector:
    app: lifecycle-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
